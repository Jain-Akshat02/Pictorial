{   
    "version": 2,
    "builds": [
        {
            "src": "server/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/auth(.*)",
            "dest": "/server/server.js"
        },
        {
            "src": "/photos(.*)",
            "dest": "/server/server.js"
        },
        {
            "src": "/images(.*)",
            "dest": "/server/server.js"
        }
    ]
}